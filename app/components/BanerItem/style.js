'use strict';

var React = require('react-native');

var {
    StyleSheet,
} = React;

module.exports = StyleSheet.create({
    baner: {
        alignItems: 'center',
        marginTop: 10,
        marginBottom: 10,
    },
});
